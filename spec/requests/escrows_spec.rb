require 'spec_helper'

describe "Escrows" do
  describe "GET /escrows" do
    it "works! (now write some real specs)" do
      get escrows_path
      expect(response.status).to be(200)
    end
  end
end
